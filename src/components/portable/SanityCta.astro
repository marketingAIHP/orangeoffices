---
const {value} = Astro.props
---
<aside class="content-cta"><h3>{value?.title}</h3>{value?.text && <p>{value.text}</p>}{value?.href && value?.label && <a class="button" href={value.href}>{value.label}</a>}</aside>
