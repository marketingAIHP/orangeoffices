---
const {node} = Astro.props
---
<aside class="content-cta"><h3>{node?.title}</h3>{node?.text && <p>{node.text}</p>}{node?.href && node?.label && <a class="button" href={node.href}>{node.label}</a>}</aside>
