<section class="workspace-card" aria-label="Plan your office space">
  <div class="workspace-card__copy">
    <p class="workspace-card__eyebrow">Your next workplace</p>
    <h2>Plan your office space</h2>
    <p>Talk to our design team about a workplace shaped around your people and business.</p>
  </div>
  <a class="workspace-card__button" href="/contact/">Discuss your project <span aria-hidden="true">→</span></a>
  <div class="workspace-card__contacts">
    <a href="tel:+917861004918"><span aria-hidden="true">☎</span> +91 78610 04918</a>
    <a href="mailto:design@orangeoffices.in"><span aria-hidden="true">✉</span> design@orangeoffices.in</a>
  </div>
</section>

<style>
  .workspace-card { display: grid; gap: 1.25rem; padding: 1.5rem; border-top: 4px solid var(--color-brand); border-radius: .7rem; background: #3f4b3f; color: var(--color-inverse); box-shadow: var(--shadow-card); }
  .workspace-card__eyebrow { margin: 0 0 .55rem !important; color: var(--color-brand) !important; font-size: .72rem !important; font-style: normal; font-weight: 700; letter-spacing: .14em; text-transform: uppercase; }
  .workspace-card h2 { margin: 0 0 .65rem; max-width: none; color: var(--color-inverse); font-size: 1.5rem; line-height: 1.15; letter-spacing: -.03em; }
  .workspace-card__copy > p:last-child { max-width: 50ch; margin: 0; font-size: 1rem; line-height: 1.55; color: rgb(255 255 255 / 82%); }
  .workspace-card__button { display: inline-flex; align-items: center; justify-content: center; justify-self: start; gap: .75rem; white-space: nowrap; padding: .85rem 1.1rem; border-radius: .35rem; background: var(--color-brand); color: var(--color-heading); font-size: .9rem; font-weight: 700; text-decoration: none; }
  .workspace-card__button:hover { background: var(--color-inverse); color: var(--color-forest); }
  .workspace-card__contacts { display: grid; gap: .65rem; padding-top: 1.1rem; border-top: 1px solid rgb(255 255 255 / 25%); }
  .workspace-card__contacts a { display: flex; gap: .5rem; color: #fff; font-size: .85rem; text-decoration: none; overflow-wrap: anywhere; }
  .workspace-card__contacts a:hover { text-decoration: underline; }
</style>
