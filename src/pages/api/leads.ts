import type { APIRoute } from 'astro'; import { z } from 'zod';
const schema = z.object({ name: z.string().trim().min(2).max(120), email: z.string().trim().email().max(254), phone: z.string().trim().max(40).optional(), message: z.string().trim().min(10).max(4000), form_id: z.literal('consultation') });
export const POST: APIRoute = async ({ request, locals }) => { const form = Object.fromEntries(await request.formData()); const parsed = schema.safeParse(form); if (!parsed.success) return new Response('Please check the form fields and try again.', { status: 400, headers: { 'cache-control': 'no-store' } }); const id = crypto.randomUUID(); const now = new Date().toISOString(); try { await locals.runtime.env.LEADS_DB.prepare('INSERT INTO lead_submissions (id,idempotency_key,form_id,source_path,created_at,updated_at,payload_ciphertext,purge_after) VALUES (?,?,?,?,?,?,?,?)').bind(id, id, parsed.data.form_id, new URL(request.url).pathname, now, now, JSON.stringify(parsed.data), new Date(Date.now() + 90 * 864e5).toISOString()).run(); await locals.runtime.env.LEAD_QUEUE.send({ id }); } catch { return new Response('We could not accept your enquiry right now. Please call us directly.', { status: 503, headers: { 'cache-control': 'no-store' } }); } return Response.redirect(new URL('/thank-you/', request.url), 303); };
