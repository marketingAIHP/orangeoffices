import type { APIRoute } from 'astro'; import { pages, site, services } from '../lib/site'; import wordpressRecords from '../data/wordpress-content.json';
export const GET: APIRoute = () => { const urls = [...new Set(['/', ...Object.keys(pages).filter((path) => path !== '/thank-you/'), ...services.map(([, href]) => href), ...wordpressRecords.map((record) => record.path)])]; const xml = `<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map((path) => `<url><loc>${site}${path}</loc></url>`).join('')}</urlset>`; return new Response(xml, { headers: { 'content-type': 'application/xml; charset=utf-8' } }); };
