import type { APIRoute } from 'astro'; import { pages, site, services } from '../lib/site'; import wordpressRecords from '../data/wordpress-content.json';
export const GET: APIRoute = () => { const postCount = wordpressRecords.filter((record) => record.type === 'post').length; const blogPages = Array.from({ length: Math.ceil(postCount / 12) - 1 }, (_, index) => `/journal/blog/page/${index + 2}/`); const urls = [...new Set(['/', ...Object.keys(pages).filter((path) => path !== '/thank-you/'), ...blogPages, ...services.map(([, href]) => href), ...wordpressRecords.map((record) => record.path)])]; const xml = `<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map((path) => `<url><loc>${site}${path}</loc></url>`).join('')}</urlset>`; return new Response(xml, { headers: { 'content-type': 'application/xml; charset=utf-8' } }); };
